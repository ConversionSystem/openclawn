{
  "name": "@openclaw/stripe-billing",
  "version": "0.1.0",
  "description": "Stripe subscription billing extension for hosted OpenClaw deployments",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "openclaw": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
